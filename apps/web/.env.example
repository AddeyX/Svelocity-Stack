# Convex deployment URL (from `npx convex dev` in packages/backend,
# or your dashboard). Local anonymous dev backend uses http://127.0.0.1:3210.
PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
