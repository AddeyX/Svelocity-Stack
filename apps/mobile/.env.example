# Same Convex deployment as web + desktop.
# NOTE: a device/emulator cannot reach 127.0.0.1 on your machine — use your
# LAN IP (http://192.168.x.x:3210) for the local backend, or a cloud deployment.
PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
