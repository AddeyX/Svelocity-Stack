# Fail install if lockfile would change in CI (CI also passes --frozen-lockfile).
engine-strict=true

# Electron tooling expects a flat-ish node_modules for its binaries.
public-hoist-pattern[]=*electron*
