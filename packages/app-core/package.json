{
	"name": "@svelocity/app-core",
	"version": "0.1.0",
	"private": true,
	"description": "Shared business logic — domain types, validation, Convex wrappers, task stores.",
	"license": "MIT",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check": "tsc -p tsconfig.json",
		"test": "vitest run"
	},
	"dependencies": {
		"zod": "catalog:"
	},
	"devDependencies": {
		"@svelocity/config": "workspace:*",
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
